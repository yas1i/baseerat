# Copy this file to .env and put your real key in it:
#     cp .env.example .env
# then edit .env and replace the placeholder. The .env file is git-ignored, so
# it is never committed or pushed. No leading '$', no surrounding quotes needed.

ANTHROPIC_API_KEY=sk-ant-your-real-key-here
